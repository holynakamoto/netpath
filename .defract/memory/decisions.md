# Past Decisions

## Decisions

- [01KWDANQ6FKMC72C61MT44ZY6D] **- **`mtr** -- - **`mtr.run()` dual return type: `list[dict]` for `passes=1`, `list[list[dict]]` for `passes>1`** — Rather than a separate `run_multi()` function, `mtr.run()` accepts `passes: int = 1`. When `passes==1` it returns the existing `list[dict]` (unchanged contract); when `passes>1` it runs mtr that many times sequentially and returns `list[list[dict]]`. Callers gate on the passes value when interpreting results. A separate function would duplicate the API surface. `_compare_as_paths(all_passes)` takes the multi-pass list and returns `{ecmp_paths, path_changes}`. [source: task-fixing-incomplete-paths-richer-path-01kwcya418n5, importance: 0.65]. [source: task-fixing-incomplete-paths-richer-path-01kwcya418n5, importance: 0.6]
- [01KWCKQ6JF8GF38HZJKGNBYM6K] **- **Exit code = worst verdict severity across all probes in a run** — For `...** -- - **Exit code = worst verdict severity across all probes in a run** — For `asn` subcommand: collects verdicts from all tested servers (or single server in json-mode) and takes the worst. For `country` subcommand: skipped ASNs (no servers, unreachable test IPs) have no `verdict` key in their summary row; `row.get("verdict")` returns None so they are excluded — no spurious non-zero exit. Rationale: monitoring scripts want a single actionable signal without parsing JSON output. [source: task-ways-to-improve-netpath-01kwc564vewr, importance: 0.65]. [source: task-ways-to-improve-netpath-01kwc564vewr, importance: 0.6]
- [01KWDAPG0C6JW86TZ6WM2SECJM] **- **`probe_count` for loss calibration is sourced from mtr `cycles` parameter...** -- - **`probe_count` for loss calibration is sourced from mtr `cycles` parameter — no new CLI surface needed** — The mtr `cycles` parameter already represents the number of probe packets sent per hop. Setting `probe_count = cycles` in `_measure()` satisfies the calibrated-loss denominator requirement without adding a `--probe-count` flag or changing the CLI contract. The three-tier thresholds in `diagnosis.py` (`LOSS_THRESHOLD_FEW=5.0` for <20, `LOSS_THRESHOLD_DEFAULT=1.0` for 20–99, `LOSS_THRESHOLD_MANY=0.5` for ≥100) are applied consistently in both the alarm trigger and the rate-limit forward-scan. [source: task-fixing-incomplete-paths-richer-path-01kwcya418n5, importance: 0.6]. [source: task-fixing-incomplete-paths-richer-path-01kwcya418n5, importance: 0.6]
- [01KWCKQA7MV650QV1RHHNSWGZP] **- **`_measure()` / `_run_test()` separation enables future JSON output in `co...** -- - **`_measure()` / `_run_test()` separation enables future JSON output in `country` mode** — `_run_test()` keeps the `json_mode` parameter only for suppressing its own display calls; `_measure()` has no `json_mode` branching and no display calls. This clean boundary makes adding `--json` to the `country` subcommand a straightforward follow-on (out of scope for this task). The original `_run_test()` lines 152–298 interleaved display with measurement via a `json_mode` flag throughout. [source: task-ways-to-improve-netpath-01kwc564vewr, importance: 0.6]. [source: task-ways-to-improve-netpath-01kwc564vewr, importance: 0.6]

