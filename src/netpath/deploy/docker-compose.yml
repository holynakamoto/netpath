# Self-hosted iperf3 server for netpath throughput measurement.
# Run:  docker compose up -d
# Then register it:  netpath serve --setup-only --advertise-host <public-host>
services:
  iperf3:
    build: .
    restart: unless-stopped
    ports:
      - "5201:5201/tcp"
      - "5201:5201/udp"
